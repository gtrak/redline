# golden for probe `bare-no-hint-bail`  (011-08 js lane)
# anchor: src/app.js:28   symbol: `greet`   scope: []   mode: deterministic
# note: bare symbol with NO import hint: the byte-for-byte no-hint bail (no scope info from the app yet).
kind: bail
message: |
  bare symbol `greet` has no package path; resolving it to a package 
  needs scope info (tree-sitter) not yet provided by the app
