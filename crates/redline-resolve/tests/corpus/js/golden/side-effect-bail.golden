# golden for probe `side-effect-bail`  (011-08 js lane)
# anchor: src/utils.mjs:8   symbol: `styles.css`   scope: []   mode: deterministic
# note: side-effect import (`import './styles.css'`): the file-ish name the app can produce is not a package spec — the dedicated file-ish bail (fix-jsrel; was the `styles`/`css` mis-split offline refusal).
kind: bail
message: |
  file-ish name `styles.css` is not a package path (a side-effect import binds no package); nothing to resolve
