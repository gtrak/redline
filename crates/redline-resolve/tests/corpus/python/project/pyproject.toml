[project]
name = "gears-demo"
version = "0.3.1"
description = "Tiny transmission kit demo (gears corpus project)"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
