# 013 — dotted from-entry (`from gears import engine`, use site
# `engine.torque` in main.py): the PATH-SHAPED symbol + the app's hint
# (["gears","engine","torque"] — original import path, item included) is
# the 011-02 alias rewrite: `engine.torque` → `gears.engine.torque`, so
# the LIVE find_spec leg lands on project/gears/engine.py (probe root on
# sys.path) and the content scan pins the `torque` definition (line 11).
# 011-08 FINDING (python probe 013) — FIXED by the fix-alias composition
# (the python provider now composes
# scope_qualified(...).or_else(scope_qualified_alias(...)) exactly like
# js_provider; the identity/mismatched/unhinted no-op cases stay pinned by
# the provider unit tests): this golden flipped from the byte-for-byte
# offline-refusal bail to the RESOLVED landing (a conscious diff).
symbol=engine.torque
from_file=main.py
scope=["gears","engine","torque"]
root=project
kind=resolved
external=false
file=project/gears/engine.py
source_root=project
line=11
