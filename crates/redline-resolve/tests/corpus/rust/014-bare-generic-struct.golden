# Bare symbol + scope hint into a plain (non-generic) struct in a sibling
# module of the widgets crate: leaf-item landing via the hint's last
# segment.
symbol = Spindle
from_file = gearbox/src/main.rs
scope = ["widgets", "spindle", "Spindle"]
kind = resolved
external = false
file = project/widgets/src/spindle.rs
source_root = project/widgets
line = 5
